import React from 'react'

function NotFound( { msg } ) {
  return (
    <div style={{
      margin: 'auto',
      fontSize: '1rem',
      fontWeight: 900
    }}>
      { msg }
    </div>
  )
}

export default NotFound
